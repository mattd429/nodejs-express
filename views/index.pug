extends layout

block content
  h1= title
  p Welcome to #[em MusicLibrary], a very basic Express website developed by Matt Brown.
  
  h1 Music Content
  
  if error
    p Error getting Music content.
  else
    p The Music library has the following record counts.
    
    ul
      li #[strong Albums:] !{data.album_count}
      li #[strong Tracks:] !{data.track_count}
      li #[strong Artists:] !{data.artist_count}
      li #[strong Genres:] !{data.genre_count}
